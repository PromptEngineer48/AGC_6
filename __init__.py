"""YouTube AI Video Pipeline v2"""
# Empty — imports handled inside each module with sys.path injection.
# This file intentionally left minimal so the package works when called
# both as  python main.py  (from inside)
# and as   python -m AGC_4.main  (from outside/parent directory).
